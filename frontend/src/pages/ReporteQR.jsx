import { useEffect, useState } from "react";
import { useParams } from "react-router-dom";
import "../styles/reporte-qr.css";
const API = import.meta.env.VITE_API_URL || "/api";
function ReporteQR(){const{token}=useParams(),[machine,setMachine]=useState(null),[form,setForm]=useState({descripcion:"",prioridad:"Media",reportado_por:"",fecha_ocurrencia:new Date().toISOString().slice(0,16)}),[file,setFile]=useState(null),[message,setMessage]=useState("");useEffect(()=>{fetch(`${API}/maquinas/por-qr/${encodeURIComponent(token)}`).then(r=>r.json()).then(setMachine).catch(()=>setMachine({error:"No se pudo leer el QR."}))},[token]);const send=async e=>{e.preventDefault();const data=new FormData();Object.entries(form).forEach(([k,v])=>data.append(k,v));data.append("id_maquina",machine.id);if(file)data.append("evidencia",file);const r=await fetch(`${API}/fallas`,{method:"POST",body:data}),b=await r.json();setMessage(r.ok?"Incidencia enviada a Mantenimiento. La máquina fue marcada como detenida.":b.error)};if(!machine)return <main className="qr-report"><p>Cargando máquina...</p></main>;if(machine.error)return <main className="qr-report"><h1>QR no válido</h1><p>{machine.error}</p></main>;return <main className="qr-report"><section><p className="eyebrow">REPORTE DE INCIDENCIA</p><h1>{machine.nombre}</h1><p>{machine.area} · Código {machine.codigo}</p><div className="qr-status">Estado actual: {machine.estado}</div>{message?<p className="qr-message">{message}</p>:<form onSubmit={send}><label>¿Qué está ocurriendo?<textarea required placeholder="Describe el síntoma o problema observado" value={form.descripcion} onChange={e=>setForm({...form,descripcion:e.target.value})}/></label><label>Fecha y hora de ocurrencia<input required type="datetime-local" value={form.fecha_ocurrencia} onChange={e=>setForm({...form,fecha_ocurrencia:e.target.value})}/></label><label>Prioridad<select value={form.prioridad} onChange={e=>setForm({...form,prioridad:e.target.value})}><option value="Baja">Baja</option><option value="Media">Media</option><option value="Alta">Alta</option><option value="Critica">Crítica</option></select></label><label>Foto de cómo se encontró<input type="file" accept="image/png,image/jpeg,image/webp" onChange={e=>setFile(e.target.files?.[0]||null)}/></label><label>Tu nombre<input required value={form.reportado_por} onChange={e=>setForm({...form,reportado_por:e.target.value})}/></label><button>Enviar incidencia</button></form>}</section></main>};export default ReporteQR;
